@model WebApplication1.ViewModels.CompanyDetailsViewModel

@{
    ViewData["Title"] = "Firma Detayları";
    ViewBag.Title = "Firma";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int computerCount = 0;
}

<div class="container-fluid mt-4">
    <!-- Header Section -->
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-action="Index">Firmalar</a></li>
            <li class="breadcrumb-item active">@Model.Company!.Name</li>
        </ol>
    </nav>
    <div class="row mb-4">
        <div class="col-12">
            <div class="card shadow-sm border-0">
                <div class="card-body bg-gradient-primary text-white">
                    <div class="row align-items-center">
                        <div class="col-md-2 text-center">
                            <div class="avatar-circle">
                                @if (!string.IsNullOrEmpty(Model.Company.LogoUrl))
                                {
                                    <img src="@Model.Company.LogoUrl" alt="@Model.Company.Name Logo" class="company-logo-small"
                                         onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';">
                                    <i class="fas fa-building fa-3x" style="display: none;"></i>
                                }
                                else
                                {
                                    <i class="fas fa-building fa-3x"></i>
                                }
                            </div>
                        </div>
                        <div class="col-md-8">
                            <h2 class="mb-1">@Model.Company.Name</h2>
                            <p class="mb-0 opacity-75">
                                <i class="fas fa-users me-2"></i>@Model.Employees!.Count Çalışan • @Model.Computers!.Count Bilgisayar
                            </p>
                            <small class="opacity-75">
                                <i class="fas fa-calendar me-1"></i>
                                Kayıt Tarihi: @Model.Company.CreatedDate.ToString("dd MMMM yyyy", new System.Globalization.CultureInfo("tr-TR"))
                            </small>
                        </div>
                        <div class="col-md-2 text-center">
                            @if (Model.Company.IsActive)
                            {
                                <span class="badge bg-success fs-6 px-3 py-2">
                                    <i class="fas fa-check-circle me-1"></i>Aktif
                                </span>
                            }
                            else
                            {
                                <span class="badge bg-secondary fs-6 px-3 py-2">
                                    <i class="fas fa-pause-circle me-1"></i>Pasif
                                </span>
                            }
                            @if (Model.Company.IsVerified)
                            {
                                <span class="badge bg-info fs-6 px-3 py-2 mt-1 d-block">
                                    <i class="fas fa-certificate me-1"></i>Onaylı
                                </span>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="row">
        <!-- Left Column - Company Information -->
        <div class="col-lg-8">
            <!-- Basic Company Information -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0">
                        <i class="fas fa-info-circle text-primary me-2"></i>
                        Firma Bilgileri
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="info-item">
                                <label class="info-label">
                                    <i class="fas fa-envelope text-muted me-2"></i>E-posta Adresi
                                </label>
                                <div class="info-value">
                                    @if (!string.IsNullOrEmpty(Model.Company.Email))
                                    {
                                        <a href="mailto:@Model.Company.Email" class="text-decoration-none">@Model.Company.Email</a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Belirtilmemiş</span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-item">
                                <label class="info-label">
                                    <i class="fas fa-phone text-muted me-2"></i>Telefon Numarası
                                </label>
                                <div class="info-value">
                                    @if (!string.IsNullOrEmpty(Model.Company.Phone))
                                    {
                                        <a href="tel:@Model.Company.Phone" class="text-decoration-none">@Model.Company.Phone</a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Belirtilmemiş</span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-item">
                                <label class="info-label">
                                    <i class="fas fa-globe text-muted me-2"></i>Website
                                </label>
                                <div class="info-value">
                                    @if (!string.IsNullOrEmpty(Model.Company.Website))
                                    {
                                        <a href="@Model.Company.Website" target="_blank" class="text-decoration-none">@Model.Company.Website</a>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Belirtilmemiş</span>
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="info-item">
                                <label class="info-label">
                                    <i class="fas fa-users text-muted me-2"></i>Çalışan Sayısı
                                </label>
                                <div class="info-value">
                                    @if (Model.Company.EmployeeCount.HasValue)
                                    {
                                        <span class="badge bg-light text-dark">@Model.Company.EmployeeCount kişi</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">Belirtilmemiş</span>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Address Information -->
            @if (!string.IsNullOrEmpty(Model.Company.Address) || !string.IsNullOrEmpty(Model.Company.City))
            {
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-header bg-white border-bottom">
                        <h5 class="mb-0">
                            <i class="fas fa-map-marker-alt text-primary me-2"></i>
                            Adres Bilgileri
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            @if (!string.IsNullOrEmpty(Model.Company.Address))
                            {
                                <div class="col-md-12">
                                    <div class="info-item">
                                        <label class="info-label">
                                            <i class="fas fa-home text-muted me-2"></i>Adres
                                        </label>
                                        <div class="info-value">@Model.Company.Address</div>
                                    </div>
                                </div>
                            }
                            <div class="col-md-6">
                                <div class="info-item">
                                    <label class="info-label">
                                        <i class="fas fa-city text-muted me-2"></i>Şehir
                                    </label>
                                    <div class="info-value">
                                        @if (!string.IsNullOrEmpty(Model.Company.City))
                                        {
                                            <span>@Model.Company.City</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Belirtilmemiş</span>
                                        }
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <label class="info-label">
                                        <i class="fas fa-map text-muted me-2"></i>İlçe
                                    </label>
                                    <div class="info-value">
                                        @if (!string.IsNullOrEmpty(Model.Company.District))
                                        {
                                            <span>@Model.Company.District</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Belirtilmemiş</span>
                                        }
                                    </div>
                                </div>
                            </div>
                            @if (!string.IsNullOrEmpty(Model.Company.PostalCode) || !string.IsNullOrEmpty(Model.Company.Country))
                            {
                                <div class="col-md-6">
                                    <div class="info-item">
                                        <label class="info-label">
                                            <i class="fas fa-mail-bulk text-muted me-2"></i>Posta Kodu
                                        </label>
                                        <div class="info-value">
                                            @if (!string.IsNullOrEmpty(Model.Company.PostalCode))
                                            {
                                                <span>@Model.Company.PostalCode</span>
                                            }
                                            else
                                            {
                                                <span class="text-muted">Belirtilmemiş</span>
                                            }
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="info-item">
                                        <label class="info-label">
                                            <i class="fas fa-flag text-muted me-2"></i>Ülke
                                        </label>
                                        <div class="info-value">
                                            @if (!string.IsNullOrEmpty(Model.Company.Country))
                                            {
                                                <span>@Model.Company.Country</span>
                                            }
                                            else
                                            {
                                                <span class="text-muted">Belirtilmemiş</span>
                                            }
                                        </div>
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            }

            <!-- Contact Person Information -->
            @if (!string.IsNullOrEmpty(Model.Company.ContactPerson))
            {
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-header bg-white border-bottom">
                        <h5 class="mb-0">
                            <i class="fas fa-user-tie text-primary me-2"></i>
                            İletişim Kişisi
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="row g-3">
                            <div class="col-md-6">
                                <div class="info-item">
                                    <label class="info-label">
                                        <i class="fas fa-user text-muted me-2"></i>Yetkili Kişi
                                    </label>
                                    <div class="info-value">
                                        <div class="d-flex align-items-center">
                                            <div class="employee-avatar me-3">
                                                @(Model.Company.ContactPerson?.Substring(0, 1).ToUpper())
                                            </div>
                                            <span>@Model.Company.ContactPerson</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <label class="info-label">
                                        <i class="fas fa-id-badge text-muted me-2"></i>Unvan
                                    </label>
                                    <div class="info-value">
                                        @if (!string.IsNullOrEmpty(Model.Company.ContactTitle))
                                        {
                                            <span>@Model.Company.ContactTitle</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Belirtilmemiş</span>
                                        }
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <label class="info-label">
                                        <i class="fas fa-envelope text-muted me-2"></i>E-posta
                                    </label>
                                    <div class="info-value">
                                        @if (!string.IsNullOrEmpty(Model.Company.ContactEmail))
                                        {
                                            <a href="mailto:@Model.Company.ContactEmail" class="text-decoration-none">@Model.Company.ContactEmail</a>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Belirtilmemiş</span>
                                        }
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-item">
                                    <label class="info-label">
                                        <i class="fas fa-phone text-muted me-2"></i>Telefon
                                    </label>
                                    <div class="info-value">
                                        @if (!string.IsNullOrEmpty(Model.Company.ContactPhone))
                                        {
                                            <a href="tel:@Model.Company.ContactPhone" class="text-decoration-none">@Model.Company.ContactPhone</a>
                                        }
                                        else
                                        {
                                            <span class="text-muted">Belirtilmemiş</span>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }

            <!-- Company Assets -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0">
                        <i class="fas fa-box text-primary me-2"></i>
                        Firmaya Ait Varlıklar
                    </h5>
                </div>
                <div class="card-body p-0">
                    <div class="accordion" id="companyAssetsAccordion">
                        <!-- Employees -->
                        <div class="accordion-item border-0">
                            <h2 class="accordion-header" id="headingEmployees">
                                <button class="accordion-button collapsed bg-light" type="button" data-bs-toggle="collapse"
                                    data-bs-target="#collapseEmployees">
                                    <i class="fas fa-users me-2 text-primary"></i>
                                    <strong>Çalışanlar</strong>
                                    <span class="badge bg-primary rounded-pill ms-auto me-2">@Model.Employees.Count</span>
                                </button>
                            </h2>
                            <div id="collapseEmployees" class="accordion-collapse collapse show"
                                data-bs-parent="#companyAssetsAccordion">
                                <div class="accordion-body">
                                    @if (Model.Employees.Any())
                                    {
                                        <div class="table-responsive">
                                            <table class="table table-hover">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th>Ad Soyad</th>
                                                        <th>E-posta</th>
                                                        <th>Telefon</th>
                                                        <th>Durum</th>
                                                        <th>İşlemler</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var employee in Model.Employees.Take(5))
                                                    {
                                                        <tr>
                                                            <td>
                                                                <div class="d-flex align-items-center">
                                                                    <div class="employee-avatar-small me-2">
                                                                        @(employee.FirstName?.Substring(0, 1).ToUpper())
                                                                    </div>
                                                                    <strong>@employee.FirstName @employee.LastName</strong>
                                                                </div>
                                                            </td>
                                                            <td>
                                                                @if (!string.IsNullOrEmpty(employee.Email))
                                                                {
                                                                    <a href="mailto:@employee.Email" class="text-decoration-none">@employee.Email</a>
                                                                }
                                                                else
                                                                {
                                                                    <span class="text-muted">-</span>
                                                                }
                                                            </td>
                                                            <td>
                                                                @if (!string.IsNullOrEmpty(employee.Phone))
                                                                {
                                                                    <a href="tel:@employee.Phone" class="text-decoration-none">@employee.Phone</a>
                                                                }
                                                                else
                                                                {
                                                                    <span class="text-muted">-</span>
                                                                }
                                                            </td>
                                                            <td>
                                                                @if (employee.IsActive)
                                                                {
                                                                    <span class="badge bg-success">Aktif</span>
                                                                }
                                                                else
                                                                {
                                                                    <span class="badge bg-secondary">Pasif</span>
                                                                }
                                                            </td>
                                                            <td>
                                                                <a asp-controller="Employee" asp-action="Details" asp-route-id="@employee.Id" 
                                                                   class="btn btn-sm btn-outline-primary">
                                                                    <i class="fas fa-eye"></i>
                                                                </a>
                                                            </td>
                                                        </tr>
                                                    }
                                                </tbody>
                                            </table>
                                            @if (Model.Employees.Count > 5)
                                            {
                                                <div class="text-center py-2">
                                                    <a asp-controller="Employee" asp-action="Index" asp-route-companyId="@Model.Company.Id" 
                                                       class="btn btn-outline-primary">
                                                        Tüm Çalışanları Gör (@Model.Employees.Count)
                                                    </a>
                                                </div>
                                            }
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="text-center py-4 text-muted">
                                            <i class="fas fa-users fa-2x mb-2"></i>
                                            <p>Bu firmada kayıtlı çalışan bulunmuyor.</p>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>

                        <!-- Computers -->
<div class="accordion-item border-0">
    <div class="accordion-header d-flex justify-content-between align-items-center px-3 pt-3">
        <button class="accordion-button collapsed flex-grow-1 bg-light rounded" type="button"
                data-bs-toggle="collapse" data-bs-target="#collapseComputers"
                aria-expanded="false" aria-controls="collapseComputers">
            <i class="fas fa-laptop me-2 text-success"></i>
            <strong>Bilgisayarlar</strong>
            @if(Model.Computers.Any())
            {
                computerCount = 0;
                @foreach(var computer in Model.Computers)
                {
                    if(computer.IsActive == true) computerCount++;
                }
            }
            <span class="badge bg-success rounded-pill ms-2">@computerCount</span>
        </button>
        <a asp-controller="Computers" asp-action="Index" asp-route-companyId="@Model.Company.Id"
           class="btn btn-outline-primary btn-sm ms-3">
            <i class="fas fa-list me-1"></i> Tümünü Gör
        </a>
    </div>
                            <div id="collapseComputers" class="accordion-collapse collapse"
                                data-bs-parent="#companyAssetsAccordion">
                                <div class="accordion-body">
                                    @if (Model.Computers.Any())
                                    {
                                        <div class="table-responsive">
                                            <table class="table table-hover">
                                                <thead class="table-light">
                                                    <tr>
                                                        <th>Bilgisayar</th>
                                                        <th>Varlık Etiketi</th>
                                                        <th>Atanan</th>
                                                        <th>Durum</th>
                                                        <th>İşlemler</th>
                                                    </tr>
                                                </thead>
                                                <tbody>
                                                    @foreach (var computer in Model.Computers.TakeLast(5))
                                                    {
                                                        if(computer.IsActive == false) continue;

                                                        <tr>
                                                            <td><strong>@computer.Name</strong></td>
                                                            <td><code>@computer.AssetTag</code></td>
                                                            <td>
                                                                @if (computer.AssignedEmployeeId.HasValue)
                                                                {
                                                                    <span class="badge bg-info">Atanmış</span>
                                                                }
                                                                else
                                                                {
                                                                    <span class="text-muted">Atanmamış</span>
                                                                }
                                                            </td>
                                                            <td><span class="badge bg-primary">@computer.Status</span></td>
                                                            <td>
                                                                <a asp-controller="Computers" asp-action="Details" asp-route-id="@computer.Id" 
                                                                   class="btn btn-sm btn-outline-primary">
                                                                    <i class="fas fa-eye"></i>
                                                                </a>
                                                            </td>
                                                        </tr>
                                                    }
                                                </tbody>
                                            </table>
                                            @if (Model.Computers.Count > 5)
                                            {
                                                <div class="text-center py-2">
                                                    <a asp-controller="Computer" asp-action="Index" asp-route-companyId="@Model.Company.Id" 
                                                       class="btn btn-outline-primary">
                                                        Tüm Bilgisayarları Gör (@Model.Computers.Count)
                                                    </a>
                                                </div>
                                            }
                                        </div>
                                    }
                                    else
                                    {
                                        <div class="text-center py-4 text-muted">
                                            <i class="fas fa-laptop fa-2x mb-2"></i>
                                            <p>Bu firmada kayıtlı bilgisayar bulunmuyor.</p>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Description/Notes -->
            @if (!string.IsNullOrEmpty(Model.Company.Description) || !string.IsNullOrEmpty(Model.Company.Notes))
            {
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-header bg-white border-bottom">
                        <h5 class="mb-0">
                            <i class="fas fa-sticky-note text-primary me-2"></i>
                            Notlar ve Açıklamalar
                        </h5>
                    </div>
                    <div class="card-body">
                        @if (!string.IsNullOrEmpty(Model.Company.Description))
                        {
                            <div class="info-item mb-3">
                                <label class="info-label">
                                    <i class="fas fa-info-circle text-muted me-2"></i>Açıklama
                                </label>
                                <div class="info-value">@Model.Company.Description</div>
                            </div>
                        }
                        @if (!string.IsNullOrEmpty(Model.Company.Notes))
                        {
                            <div class="info-item">
                                <label class="info-label">
                                    <i class="fas fa-sticky-note text-muted me-2"></i>Notlar
                                </label>
                                <div class="info-value">@Model.Company.Notes</div>
                            </div>
                        }
                    </div>
                </div>
            }
        </div>

        <!-- Right Column - Quick Actions & Statistics -->
        <div class="col-lg-4">
            <!-- Company Logo -->
            @if (!string.IsNullOrEmpty(Model.Company.LogoUrl))
            {
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-header bg-white border-bottom">
                        <h5 class="mb-0">
                            <i class="fas fa-image text-primary me-2"></i>
                            Firma Logosu
                        </h5>
                    </div>
                    <div class="card-body text-center">
                        <img src="@Model.Company.LogoUrl" alt="@Model.Company.Name Logo" class="img-fluid company-logo-large"
                             onerror="this.style.display='none'; this.nextElementSibling.style.display='block';">
                        <div style="display: none;" class="logo-fallback">
                            <i class="fas fa-building fa-4x text-primary mb-2"></i>
                            <p class="text-muted">Logo yüklenemedi</p>
                        </div>
                    </div>
                </div>
            }

            <!-- Quick Actions -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0">
                        <i class="fas fa-bolt text-primary me-2"></i>
                        Hızlı İşlemler
                    </h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a asp-action="Edit" asp-route-id="@Model.Company.Id" class="btn btn-warning btn-lg">
                            <i class="fas fa-edit me-2"></i>Firmayı Düzenle
                        </a>

                        @if (!string.IsNullOrEmpty(Model.Company.Email))
                        {
                            <a href="mailto:@Model.Company.Email" class="btn btn-outline-primary">
                                <i class="fas fa-envelope me-2"></i>E-posta Gönder
                            </a>
                        }

                        @if (!string.IsNullOrEmpty(Model.Company.Phone))
                        {
                            <a href="tel:@Model.Company.Phone" class="btn btn-outline-success">
                                <i class="fas fa-phone me-2"></i>Ara
                            </a>
                        }

                        <a asp-controller="Employee" asp-action="Create" asp-route-companyId="@Model.Company.Id"
                           class="btn btn-outline-primary">
                            <i class="fas fa-user-plus me-2"></i>Çalışan Ekle
                        </a>

                        <a asp-controller="Computer" asp-action="Create" asp-route-companyId="@Model.Company.Id"
                           class="btn btn-outline-success">
                            <i class="fas fa-laptop me-2"></i>Bilgisayar Ekle
                        </a>

                        <button type="button" class="btn btn-outline-info" onclick="generateReport()">
                            <i class="fas fa-file-alt me-2"></i>Rapor Oluştur
                        </button>

                        <button type="button" class="btn btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteModal">
                            <i class="fas fa-trash me-2"></i>Sil
                        </button>
                    </div>
                </div>
            </div>

            <!-- Statistics Card -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0">
                        <i class="fas fa-chart-bar text-primary me-2"></i>
                        Varlık İstatistikleri
                    </h5>
                </div>
                <div class="card-body">
                    <div class="row text-center">
                        <div class="col-6">
                            <div class="stat-item">
                                <h4 class="text-primary mb-0">@Model.Employees.Count</h4>
                                <small class="text-muted">Çalışan</small>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-item">
                                <h4 class="text-success mb-0">@Model.Computers.Count</h4>
                                <small class="text-muted">Bilgisayar</small>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-item">
                                <h4 class="text-warning mb-0">@Model.Software!.Count</h4>
                                <small class="text-muted">Yazılım</small>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-item">
                                <h4 class="text-info mb-0">@Model.Supplies!.Count</h4>
                                <small class="text-muted">Sarf Malzeme</small>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-item">
                                <h4 class="text-secondary mb-0">@((DateTime.Now - Model.Company.CreatedDate).Days)</h4>
                                <small class="text-muted">Gün Önce Eklendi</small>
                            </div>
                        </div>
                        <div class="col-6">
                            <div class="stat-item">
                                <h4 class="text-primary mb-0">
                                    @(Model.Company.IsActive ? "✓" : "✗")
                                </h4>
                                <small class="text-muted">Durum</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Activity Timeline - UPDATED SECTION -->
            <!-- Activity Timeline - Simplest Version -->
<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-white border-bottom">
        <h5 class="mb-0">
            <i class="fas fa-history text-primary me-2"></i>
            Firma Aktiviteleri
        </h5>
    </div>
    <div class="card-body p-0">
        <div class="timeline-logs px-3 py-2">
            @if (Model.ActivityLogs != null && Model.ActivityLogs.Any())
            {
                @foreach (var log in Model.ActivityLogs)
                {
                    var uniqueId = Guid.NewGuid().ToString("N");
                    <div class="log-entry border-bottom pb-2 mb-2">
                        <div class="d-flex justify-content-between align-items-start">
                            <div class="flex-grow-1">
                                <div class="fw-bold">
                                    @switch(log.Action.ToUpper())
                                    {
                                        case "CREATE":
                                            <i class="fas fa-plus-circle text-success me-1"></i>
                                            <span>Firma oluşturuldu</span>
                                            break;
                                        case "UPDATE":
                                            <i class="fas fa-edit text-warning me-1"></i>
                                            <span>Firma güncellendi</span>
                                            break;
                                        case "DELETE":
                                            <i class="fas fa-trash text-danger me-1"></i>
                                            <span>Firma silindi</span>
                                            break;
                                        case "ACTIVATE":
                                            <i class="fas fa-check-circle text-success me-1"></i>
                                            <span>Firma aktif edildi</span>
                                            break;
                                        case "DEACTIVATE":
                                            <i class="fas fa-times-circle text-secondary me-1"></i>
                                            <span>Firma pasif edildi</span>
                                            break;
                                        case "ADD_EMPLOYEE":
                                            <i class="fas fa-user-plus text-info me-1"></i>
                                            <span>Çalışan eklendi</span>
                                            break;
                                        case "REMOVE_EMPLOYEE":
                                            <i class="fas fa-user-minus text-warning me-1"></i>
                                            <span>Çalışan çıkarıldı</span>
                                            break;
                                        case "ADD_COMPUTER":
                                            <i class="fas fa-laptop text-primary me-1"></i>
                                            <span>Bilgisayar eklendi</span>
                                            break;
                                        case "REMOVE_COMPUTER":
                                            <i class="fas fa-laptop text-danger me-1"></i>
                                            <span>Bilgisayar çıkarıldı</span>
                                            break;
                                        default:
                                            <i class="fas fa-info-circle text-secondary me-1"></i>
                                            <span>@log.Action</span>
                                            break;
                                    }
                                </div>
                            </div>
                        </div>
                        <div class="d-flex justify-content-between align-items-center mt-1">
                            <div class="text-muted small">
                                <i class="fas fa-user me-1"></i>
                                @(log.User?.Email ?? "Bilinmiyor")
                            </div>
                            <div class="text-muted small">
                                <i class="fas fa-clock me-1"></i>
                                @log.CreatedDate.ToLocalTime().ToString("dd.MM.yyyy HH:mm")
                            </div>
                        </div>
                        @if (!string.IsNullOrEmpty(log.Detail))
                        {
                            <div class="mt-1">
                                <a href="#" class="text-primary small" onclick="document.getElementById('@uniqueId').style.display='block'; this.style.display='none'; document.getElementById('hide-@uniqueId').style.display='inline'; return false;">
                                    <i class="fas fa-eye me-1"></i>Detayları göster
                                </a>
                                <a href="#" id="hide-@uniqueId" class="text-primary small" style="display:none;" onclick="document.getElementById('@uniqueId').style.display='none'; this.style.display='none'; this.previousElementSibling.style.display='inline'; return false;">
                                    <i class="fas fa-eye-slash me-1"></i>Detayları gizle
                                </a>
                            </div>
                            <div id="@uniqueId" class="mt-2 p-2 bg-light rounded" style="display: none; border-left: 3px solid #007bff;">
                                <small class="text-muted" style="white-space: pre-line;">@log.Detail</small>
                            </div>
                        }
                    </div>
                }
            }
        </div>
        @if (Model.ActivityLogs != null && Model.ActivityLogs.Count >= 10)
        {
            <div class="text-center py-2 bg-light">
                <a asp-controller="ActivityLog" asp-action="Index" asp-route-entityType="Company" asp-route-entityId="@Model.Company.Id" 
                   class="btn btn-sm btn-outline-primary">
                    <i class="fas fa-list me-1"></i>Tüm Aktiviteleri Gör
                </a>
            </div>
        }
    </div>
</div>

            <!-- System Information -->
            <div class="card shadow-sm border-0 mb-4">
                <div class="card-header bg-white border-bottom">
                    <h5 class="mb-0">
                        <i class="fas fa-cog text-primary me-2"></i>
                        Sistem Bilgileri
                    </h5>
                </div>
                <div class="card-body">
                    <div class="info-item">
                        <label class="info-label">
                            <i class="fas fa-plus-circle text-muted me-2"></i>Oluşturan
                        </label>
                        <div class="info-value">@(Model.Company.CreatedBy ?? "Sistem")</div>
                    </div>
                    @if (!string.IsNullOrEmpty(Model.Company.UpdatedBy))
                    {
                        <div class="info-item mt-3">
                            <label class="info-label">
                                <i class="fas fa-edit text-muted me-2"></i>Son Güncelleyen
                            </label>
                            <div class="info-value">@Model.Company.UpdatedBy</div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <!-- Navigation -->
    <div class="row mt-4">
        <div class="col-12">
            <div class="d-flex justify-content-between">
                <a asp-action="Index" class="btn btn-outline-secondary btn-lg">
                    <i class="fas fa-arrow-left me-2"></i>Firma Listesi
                </a>
                <div>
                    <a asp-action="Edit" asp-route-id="@Model.Company.Id" class="btn btn-primary btn-lg me-2">
                        <i class="fas fa-edit me-2"></i>Düzenle
                    </a>
                    <button type="button" class="btn btn-outline-info btn-lg me-2" onclick="window.print()">
                        <i class="fas fa-print me-2"></i>Yazdır
                    </button>
                    <button type="button" class="btn btn-outline-danger btn-lg" data-bs-toggle="modal" data-bs-target="#deleteModal">
                        <i class="fas fa-trash me-2"></i>Sil
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    Firmayı Sil
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p><strong>@Model.Company.Name</strong> adlı firmayı silmek istediğinizden emin misiniz?</p>
                <p class="text-muted">Bu işlem firmaya ait tüm çalışan ve varlık kayıtlarını da silecektir ve geri alınamaz.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
                <form asp-action="Delete" asp-route-id="@Model.Company.Id" method="post" class="d-inline">
                    <button type="submit" class="btn btn-danger">
                        <i class="fas fa-trash me-1"></i>Evet, Sil
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // Generate report function
        function generateReport() {
            Swal.fire({
                title: 'Rapor Türü Seçin',
                text: 'Hangi raporu oluşturmak istiyorsunuz?',
                icon: 'question',
                showCancelButton: true,
                showDenyButton: true,
                confirmButtonText: 'Detaylı Rapor',
                denyButtonText: 'Özet Rapor',
                cancelButtonText: 'İptal',
                confirmButtonColor: '#007bff',
                denyButtonColor: '#28a745',
                cancelButtonColor: '#6c757d'
            }).then((result) => {
                if (result.isConfirmed) {
                    window.open('/Company/DetailedReport/@Model.Company.Id', '_blank');
                } else if (result.isDenied) {
                    window.open('/Company/SummaryReport/@Model.Company.Id', '_blank');
                }
            });
        }

        // Toggle detail function for activity logs
        function toggleDetail(detailId) {
            var element = document.getElementById(detailId);
            if (element) {
                if (element.style.display === 'none') {
                    element.style.display = 'block';
                } else {
                    element.style.display = 'none';
                }
            }
        }

        // Auto expand first accordion if there are items
        document.addEventListener('DOMContentLoaded', function () {
            // Logo error handling
            const logoImg = document.querySelector('.company-logo-large');
            if (logoImg) {
                logoImg.addEventListener('error', function () {
                    this.style.display = 'none';
                    const fallback = this.nextElementSibling;
                    if (fallback) {
                        fallback.style.display = 'block';
                    }
                });
            }
        });
    </script>
}

<style>
    .bg-gradient-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .avatar-circle {
        width: 80px;
        height: 80px;
        background: rgba(255, 255, 255, 0.2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto;
    }

    .company-logo-small {
        width: 70px;
        height: 70px;
        object-fit: contain;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.9);
        padding: 5px;
    }

    .company-logo-large {
        max-height: 150px;
        max-width: 100%;
        object-fit: contain;
    }

    .logo-fallback {
        padding: 2rem;
        background: #f8f9fa;
        border-radius: 10px;
    }

    .info-item {
        padding: 15px;
        border-radius: 8px;
        background: #f8f9fa;
        height: 100%;
    }

    .info-label {
        font-size: 0.875rem;
        font-weight: 600;
        color: #6c757d;
        margin-bottom: 8px;
        display: block;
    }

    .info-value {
        font-size: 1rem;
        font-weight: 500;
        color: #212529;
    }

    /* Updated Timeline Styles */
    .timeline-logs {
        max-height: 300px;
        overflow-y: auto;
        background: #f7fafc;
        border-radius: 0 0 12px 12px;
    }

    .timeline-logs::-webkit-scrollbar {
        width: 10px;
        background: #e2e8f0;
        border-radius: 10px;
    }

    .timeline-logs::-webkit-scrollbar-thumb {
        background: linear-gradient(160deg, #007bff 30%, #54a0ff 100%);
        border-radius: 10px;
    }

    .timeline-logs::-webkit-scrollbar-thumb:hover {
        background: linear-gradient(180deg, #0056b3, #007bff);
    }

    .log-entry {
        transition: all 0.3s ease;
        padding: 12px 8px;
        border-radius: 8px;
        margin-bottom: 8px;
    }

    .log-entry:hover {
        background: #e8f0fe;
        transform: translateX(5px);
    }

    .log-entry:last-child {
        border-bottom: none !important;
        margin-bottom: 0;
        padding-bottom: 8px;
    }

    /* Icon colors for activity types */
    .log-entry .fa-plus-circle { color: #28a745; }
    .log-entry .fa-edit { color: #ffc107; }
    .log-entry .fa-trash { color: #dc3545; }
    .log-entry .fa-check-circle { color: #28a745; }
    .log-entry .fa-times-circle { color: #6c757d; }
    .log-entry .fa-user-plus { color: #17a2b8; }
    .log-entry .fa-user-minus { color: #fd7e14; }
    .log-entry .fa-laptop { color: #007bff; }

    /* Animation for new log entries */
    @@keyframes slideIn {
        from {
            opacity: 0;
            transform: translateX(-20px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    .log-entry {
        animation: slideIn 0.3s ease-out;
    }

    /* Simple styles for clickable logs */
    .log-entry.has-detail:hover {
        background: #e3ebf6 !important;
    }

    .log-detail-content {
        border-left: 3px solid #007bff;
        background: #f0f4f8 !important;
    }

    .stat-item {
        padding: 10px;
    }

    .employee-avatar {
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        color: white;
        font-size: 0.875rem;
    }

    .employee-avatar-small {
        width: 30px;
        height: 30px;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        color: white;
        font-size: 0.75rem;
    }

    .accordion-button {
        border: none;
        border-radius: 8px !important;
        margin-bottom: 5px;
    }

    .accordion-button:not(.collapsed) {
        background-color: #f8f9fa;
        border-color: #dee2e6;
        color: #212529;
    }

    .accordion-item {
        border: none;
    }

    .table th {
        border-top: none;
        font-weight: 600;
        font-size: 0.875rem;
        background-color: #f8f9fa !important;
    }

    .table-hover tbody tr:hover {
        background-color: #f5f5f5;
    }

    /* Responsive adjustments */
    @@media (max-width: 768px) {
        .log-entry {
            font-size: 0.875rem;
        }
        
        .timeline-logs {
            max-height: 200px;
        }
    }
</style>